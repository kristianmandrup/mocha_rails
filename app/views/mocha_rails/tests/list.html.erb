<% if @mocha_suites.length == 0 %>
  <div class="test fail">
    There are currently no test suites
  </div>
<% else %>
  <div class="suite">
  <% @mocha_suites.each do |file,text| %>
    <h1><%= link_to text, mocha_test_page_path(:suite => CGI.escape(file)) %></h1>
  <% end %>
  </div>
<% end %>
